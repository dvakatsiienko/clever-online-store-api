{
    "extends": "tsconfig-dva",

    /* File inclusion */
    "exclude": ["node_modules"],
    "include": ["**/*.ts"],

    "compilerOptions": {
        /* Module resolution */
        "baseUrl": ".",
        "paths": { "@/*": ["src/*"] }
    }
}
